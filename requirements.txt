# Cross-platform development and test environment dependencies.
# These are intentionally explicit so a Windows or Linux test virtual
# environment can be created from a checkout without installing the project
# by local path. pyproject.toml remains authoritative for package metadata.

# Runtime
keyring>=25
defusedxml>=0.7
cryptography>=44
paramiko>=3.4
playwright>=1.53

# Build and quality gates
build>=1.2
pytest>=8
ruff>=0.5
mypy>=1.10
types-defusedxml>=0.7
